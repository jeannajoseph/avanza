<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Avanza Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <style>
    .blog-header-img {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      border-radius: 10px;
    }
    .blog-content {
      font-size: 1.1rem;
      line-height: 1.8;
      color: #333;
    }
    .blog-content img {
      max-width: 100%;
      height: auto;
      border-radius: 5px;
      margin: 20px 0;
    }
  </style>
</head>
<body>

  <%- include('../partials/navbar') %>

  <main class="py-5">
    <div class="container" style="max-width: 800px;">
      
      <a href="/blog" class="text-decoration-none mb-4 d-inline-block">&larr; Back to Blog</a>

      <article>
        <header class="mb-4">
          <h1 class="fw-bold display-5 mb-3"><%= post.title %></h1>
          
          <div class="d-flex align-items-center text-muted mb-4">
            <% if (post.author && post.author.profileImage) { %>
              <img src="<%= post.author.profileImage %>" alt="<%= post.author.firstName %>" class="rounded-circle me-2" width="40" height="40">
            <% } %>
            <div>
              <span class="fw-bold text-dark">
                <%= post.author ? post.author.firstName + ' ' + post.author.lastName : 'Avanza Team' %>
              </span>
              <span class="mx-2">•</span>
              <span><%= post.publishedAt.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
            </div>
          </div>

          <% if (post.coverImage) { %>
            <img src="<%= post.coverImage %>" alt="<%= post.title %>" class="blog-header-img shadow-sm mb-4">
          <% } %>
        </header>

        <div class="blog-content">
          <%- post.content %>
        </div>
      </article>

      <% if (post.author && post.author.bio) { %>
        <hr class="my-5">
        <div class="bg-light p-4 rounded d-flex align-items-start">
          <% if (post.author.profileImage) { %>
            <img src="<%= post.author.profileImage %>" class="rounded-circle me-3" width="60" height="60" alt="<%= post.author.firstName %>">
          <% } %>
          <div>
            <h5 class="fw-bold mb-1">About <%= post.author.firstName %></h5>
            <p class="mb-0 text-muted"><%= post.author.bio %></p>
          </div>
        </div>
      <% } %>

    </div>
  </main>

  <%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>