<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Avanza Education</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <style>
    .blog-card-img {
      height: 200px;
      object-fit: cover;
    }
  </style>
</head>
<body>

  <%- include('../partials/navbar') %>

  <main class="py-5 bg-light">
    <div class="container">
      <div class="text-center mb-5">
        <h1 class="fw-bold">Avanza Blog</h1>
        <p class="text-muted">Insights and strategies for your college journey</p>
      </div>

      <div class="row g-4">
        <% if (posts.length > 0) { %>
          <% posts.forEach(post => { %>
            <div class="col-md-4">
              <div class="card h-100 shadow-sm border-0">
                <% if (post.coverImage) { %>
                  <img src="<%= post.coverImage %>" class="card-img-top blog-card-img" alt="<%= post.title %>">
                <% } else { %>
                  <div class="card-img-top blog-card-img bg-secondary d-flex align-items-center justify-content-center text-white">
                    <span>Avanza Education</span>
                  </div>
                <% } %>
                
                <div class="card-body d-flex flex-column">
                  <h5 class="card-title fw-bold"><%= post.title %></h5>
                  <p class="card-text text-muted small mb-2">
                    <% if (post.author) { %>
                      By <%= post.author.firstName %> <%= post.author.lastName %>
                    <% } else { %>
                      By Avanza Team
                    <% } %>
                    • <%= post.publishedAt.toLocaleDateString() %>
                  </p>
                  
                  <div class="mt-auto pt-3">
                    <a href="/blog/<%= post.slug %>" class="btn btn-outline-primary stretched-link">Read Article</a>
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <div class="col-12 text-center py-5">
            <h3 class="text-muted">No posts found yet. Check back soon!</h3>
          </div>
        <% } %>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>